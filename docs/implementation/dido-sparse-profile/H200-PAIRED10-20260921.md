# H200 continuation: ten matched pairs
Status: PREDECLARED, 2026-09-21 UTC (2026-09-20 America/Los_Angeles).
The user explicitly requests local add/commit/push, then testing on
H200-target-server, and clarified ten pairs: ten Dense plus ten candidate
episodes, **20 new attempts**. No new H100 rollout was started. The earlier
12-attempt H100 study remains intact and separate; this continuation brings
the effort to at most 32 attempts under the original 50-total cap.

## Frozen protocol and allocation
Use unchanged model `41f515a5480cdcc87a18b147ab393e9cae337e9f`, strengthened
Dense and layerwise native value-aware R56, D0/R1–9 feature reuse. No retuning,
new selector, training, checkpoint, dependency or protocol change.
`dreamwam-release-v1`: all ten Spatial tasks, initial state 3, repeat 0, seed42,
400 maximum steps, 30 waits, 256×256 cameras, replan10, horizon32, denoising10.
These identities are not globally unseen: historical full baselines cover them.

| Lane | Tasks | Policy | Dedicated EGL renderer | First arm |
|---|---|---|---|---|
| a | 0–4 | GPU2 | GPU3 | Dense |
| b | 5–9 | GPU6 | GPU7 | Candidate |

Each lane runs both arms on the same policy/renderer pair, five episodes each.
The user corrected allocation to groups2/3 and6/7 before any GPU launch.
Leave GPU5 unused. Check free memory/utilization before every arm; require empty
renderers, no foreign process signals and retries0/error_policy stop. Existing
bounded launcher handles each lane and serializes their common ledger.
Estimated closed-loop time10–20min in parallel; limits900s/arm,120s admission,
120s cleanup,35min outer timeout. Stop after these twenty attempts; no expansion.
A task failure is an outcome; a renderer/model error is not a task failure.

## Environment and gates
Reuse H200 model Python3.10.20/torch2.7.1+cu126 and existing evaluator
Python3.10.20, MuJoCo3.3.2, robosuite1.4.1, numpy1.26.4. Evaluator torch
2.7.1+cu128 is its existing simulator environment, not a replacement of model
torch. Preserve LIBERO8f1084e and its existing legacy-pickle `weights_only=False`
compatibility patch; record hashes without overwriting it.

Use clean isolated model/evaluator branches, then detached immutable run trees.
Model sync entrypoint: `bash deployment/h200/sync-dido-worktree.sh`.
Before rollout, verify checkpoint SHA256
`6c087e5b9e201f19dbe92834b470a7ee18937a18ea9adcbb49886aa1e3ac4c61`,
both configs in each lane, native RGB on each empty dedicated renderer, CPU/CUDA
checks and the frozen adapter on this H200 environment. Adapter budget22 complete
predictions on three existing captured inputs; renderer diagnostics load no policy
and produce no SR outcomes. These checks do not add closed-loop episode attempts.

## One conserved effort budget across the host change
Transfer the **entire finalized** H100 ledger, byte/hash verified, including its
12 charges and original records, to
`/data/chenjiayu/wenbiao_zhao/dreamwam-sr/outputs/dido-sparse-profile-20260920/closed-loop-ledger.json`.
H200 becomes the sole writable ledger for this continuation; preserve the old H100
snapshot and record a relocation receipt there. No empty replacement ledger,
refunded slot or simultaneous H100 writer. Both full five-pair manifests reserve
ten slots each before their first evaluator process: total32, never above50.
Record the transfer hash and both final receipts with raw evidence.

## Reporting
H200/EGL is a separate hardware/rendering cohort from H100/OSMesa; do not pool SR
or timing. Audit initial images/state, prediction prefixes, native reads, budgets,
all outcome/error records, action archives and process release. Report Dense and
candidate outcomes on these ten pairs, Wilson intervals and paired uncertainty.
The user's allowable SR drop is5percentage points; a tiny result alone does not
certify that margin. Preserve prior adverse offline selection evidence.
