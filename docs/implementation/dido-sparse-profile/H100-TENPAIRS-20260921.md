# H100 continuation: ten matched pairs

Predeclared 2026-09-21 UTC. The user redirected the unstarted H200 continuation
to H100 after reserving GPUs3/4, requiring release and real launch in one command
and rapid re-holding after completion. Reuse H100's verified private CPU OSMesa
renderer, frozen model41f515a and original checkpoint. No EGL dependency change,
method tuning, training or new model adapter check is needed.

Lane a: tasks0–4, GPU3, Dense first; lane b: tasks5–9, GPU4, candidate first.
Both run concurrently with CPU rendering. Each arm has five episodes; total
ten Dense + ten frozen native layerwise value-aware R56 episodes. Initial state3,
repeat0, seed42, unchanged dreamwam-release-v1:400 steps,30 waits,256×256,
replan10,horizon32,denoising10. No retry or automatic expansion. Leave GPU5
unused by this task. H100 timestamps are recorded explicitly in UTC despite
the SSH host's default UTC+8 display.

The H200 ledger remained unchanged at12 charges/12 actual attempts, SHA256
73501eee4d96b2419684a8a1cf35b54cd86260089d9eef0a28f5df8c98ce4247.
Verify the identical H100 snapshot and both hosts' finalized records; write
relocation receipts before making H100 sole writer again. Both lanes use the
same original H100 ledger, locked per reservation. Twenty new slots imply32
total under the50-attempt cap. H200's failed renderer diagnostics add no episode.

Prepare and validate all four configs, checkpoint hash, native OSMesa readback,
new controller CPU checks and exact immutable source while3/4 remain held.
The committed run-dido-tenpairs.sh verifies the owned hold identities, stops
the two workers from gpu-hold/h100-gpu.json, and immediately forks both lanes.
Each completed lane attempts a fresh120-minute hold on its own card, for up to
30 seconds of release/admission polling, without touching foreign processes.
New hold state files: gpu-hold/h100-tenpairs-after-gpu3.json and ...gpu4.json.
A resource conflict is reported, never forcibly displaced.

User's no-total-wall-limit preference is implemented with --wall-seconds0,
permitted only with explicit configs and the conserved episode ledger. Startup,
prediction and cleanup error detection stay active; this does not enlarge the
step or episode limits. No outer wall timeout. Preserve raw outcomes and count
errors separately. Verify native reads, actual actions, initial input pairs,
timing coverage and final ledger. Report H100 ten-pair results separately from
historical cohorts; ten pairs alone cannot certify a five-percentage-point margin.
