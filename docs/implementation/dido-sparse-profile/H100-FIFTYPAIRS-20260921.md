# H100 50-pair exploratory comparison

Status: PREDECLARED, 2026-09-21 UTC. No outcomes available at declaration.

User authorization: “50对：Dense与候选各50条，共100次新增”. This updates the
original cumulative50 limit to132: the existing32 attempts remain conserved,
and exactly100 new attempts are authorized. The existing ledger is amended
with the user quote, timestamp and prior-file SHA256; no reset/refund/new ledger.

## Fixed design

Spatial tasks0–9 × initial states4–8 × repeat0, seed42:50 identities per arm.
States differ from the immediately preceding init3 ten-pair cohort; do not call
them globally unseen, and do not pool the separately requested cohorts.

| Lane | H100 GPU | Tasks | Initial states | Arm order | Attempts |
|---|---:|---|---|---|---:|
| a | 3 | 0–4 | 4,5,6,7,8 | Dense then candidate | 50 |
| b | 4 | 5–9 | 4,5,6,7,8 | Candidate then Dense | 50 |

Both lanes use verified private CPU OSMesa; GPU5 is left unused by this task.
Fresh admission and process ownership checks remain mandatory. The current
holds are `gpu-hold/h100-tenpairs-after-gpu{3,4}.json`. Only these verified owned
workers are released. Deployment entrypoint is
`deployment/h100/run-dido-fiftypairs.sh FROZEN_EVAL_ROOT OUTPUT_ROOT`.
It releases and starts both lanes in one shell, then attempts to re-hold each
card immediately after its lane completes. New120-minute hold states are
`gpu-hold/h100-fiftypairs-after-gpu{3,4}.json`. Preserve all foreign-process checks.

Frozen model:41f515a5480cdcc87a18b147ab393e9cae337e9f; checkpoint SHA256
6c087e5b9e201f19dbe92834b470a7ee18937a18ea9adcbb49886aa1e3ac4c61.
Dense retains conditioned-frame caching and full transformer CUDA graphs.
Candidate remains layerwise native value_action R56, D0/R1–9 feature reuse.
No retuning after the previous9/10 versus8/10 observation.

Protocol remains dreamwam-release-v1:400max steps,30wait steps,10executed actions
per prediction,256² dual cameras; action horizon32,10denoising steps. No whole-arm
wall deadline (`--wall-seconds 0`), but startup/prediction/cleanup controls remain.
Retries0, error_policy stop; failures are benchmark-owned, errors stay separate.
Existing environments and simulator versions are reused without installs.

Expected execution time disclosed before launch:35–50minutes, depending on episode
length and host load. The preceding20-attempt cohort took8m43s in parallel.

## Verification and reporting

Before launch: validate/doctor all four frozen evaluator configs, check source and
checkpoint hashes, test ledger amendments and concurrent reservations on H100,
confirm the previously verified native renderer remains healthy. Do not add a
closed-loop smoke outside the100-attempt manifest. Preserve per-run immutable trees.

After completion: audit all terminal outcomes, native reads, execution prefixes,
initial input matching, effective method fingerprints and artifact hashes. Use
`audit_dido_tenpairs.py --initial-states 4 5 6 7 8 --expected-cap 132` to aggregate
both disjoint lanes. Report each arm's SR/Wilson interval, paired difference with
task-stratified paired bootstrap10000resamples/seed42 and exact McNemar statistics.
Observed drop tolerance remains5percentage points. Do not equate observed acceptance
with proof of a population non-inferiority margin.

Report full warm predict_action mean/median/p95, episode-balanced latency and
whole-episode wall time; the closed-loop latency ratio is descriptive because inputs
and episode lengths can diverge. Also record controller elapsed time and immediate
re-hold delay. Never report a complete SR from incomplete coverage.

Outcome/provenance records will include exact source revisions, UTC times, executed
commands, exit codes, checkpoint/config/manifest/artifact hashes, coverage, cumulative
charges/attempts and limitations. Next step after verification: stop evaluation,
preserve finite holds and report results; no automatic extension or tuning.
