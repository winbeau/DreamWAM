# H100 十对评测结果

状态：完成，2026-09-21 03:11:00 UTC（洛杉矶时间 2026-09-20 20:11）。
本轮固定候选的 SR 比 Dense 低 **10 个百分点**，超过用户允许的 5 点，
不通过本轮验收。没有追加测试或根据结果调参。

| 指标 | Dense | 冻结候选 |
|---|---:|---:|
| 成功 | 9/10（90%） | 8/10（80%） |
| SR Wilson 95% 区间 | 59.58%–98.21% | 49.02%–94.33% |
| 热态完整推理均值 | 265.45 ms | 133.10 ms |
| 热态推理样本数 | 123 | 168 |
| 每 episode 总耗时均值 | 36.83 s | 43.95 s |

描述性热态推理加速 **1.994×**；按 episode 等权后为 **1.990×**。
实际 episode 总耗时增加 **19.34%**，因此推理提速没有变成本轮整体提速。
热态定义沿用既有工具：每个 episode 排除第一次调用，并要求 prompt-cache hit。
模型耗时包含完整 predict 与 CPU action 输出，不包含加载、IPC、物理模拟及渲染。
闭环观测和轨迹长度不同，这个比值不是同输入回放的受控加速比。

## 逐任务结果

全部使用 Spatial，init3、repeat0、seed42。括号内为实际动作步数；失败均为
达到400步，均是 benchmark 判定的任务失败，不是基础设施错误。

| Task | Dense | 候选 |
|---|---|---|
| 0 | 成功（78） | 成功（186） |
| 1 | 成功（102） | 失败（400） |
| 2 | 成功（96） | 成功（105） |
| 3 | 成功（80） | 成功（94） |
| 4 | 成功（118） | 失败（400） |
| 5 | 失败（400） | 成功（95） |
| 6 | 成功（98） | 成功（102） |
| 7 | 成功（116） | 成功（118） |
| 8 | 成功（92） | 成功（107） |
| 9 | 成功（114） | 成功（137） |

配对结果为 Dense 独有成功2例、候选独有成功1例、双方成功7例。
精确 McNemar 双侧 p=1.0。每个任务只有一个 episode，任务分层 bootstrap
区间留空，不能将重复抽样产生的零宽区间当作证据。本轮观测未满足5点上限；
样本不足以断言总体 SR 必然下降10点，也不能证明5点非劣性。

## 执行与核验

GPU3：tasks0–4，Dense先；GPU4：tasks5–9，候选先。两组并行，均使用 H100
已有的私有 CPU OSMesa 环境。协议保持 dreamwam-release-v1：400最大步数、
30等待步、256×256、replan10、horizon32、denoising10。冻结候选仍为逐层
value-action R56，D0/R1–9 特征复用；未改变模型、checkpoint、方法或依赖。

本轮 **20/20 次完成，0错误、0重试、0强制清理**；累计 **32次实际尝试 /
32个已计费名额**，没有填满50次上限。H200的两次渲染诊断未启动闭环，账本
在双端核对字节哈希后返回 H100。旧H100队列与历史结果没有重启或混合统计。

预检：四份配置 validate/doctor、16项控制器/预算测试、80次原生 OSMesa
读取通过，checkpoint 重算哈希一致。模型继续使用既有41f515a适配器验证。
完整审计复核 **523个产物哈希、7476次原生读取、20份执行动作与预测前缀**；
10对初始 state、agentview、wrist 哈希全部一致。所有推理进程已退出。

固定版本：模型 `41f515a5480cdcc87a18b147ab393e9cae337e9f`；评测器
`91b0765fc94836ab519cd5049c488460d5e597b3`；控制器
`dcc8af80c7a8e5fa10af8a99666ca6e4d3875864`；最终审计
`d79e7db7498ba0607a6d1e1c87946a31e8523f6a`。Checkpoint SHA256：
`6c087e5b9e201f19dbe92834b470a7ee18937a18ea9adcbb49886aa1e3ac4c61`。

启动命令为固定控制器中的 `deployment/h100/run-dido-tenpairs.sh`，原始记录在
`h100-server:/root/wenbiao_zhao/dreamwam-sr/outputs/dido-h100-paired10-20260921/`。
入口、每臂实际命令、时间、退出码、环境、账本与产物哈希见
[完整验证记录](H100-TENPAIRS-VERIFICATION.json)；统计及逐任务记录见
[结果 JSON](H100-TENPAIRS-RESULTS.json)。最初聚合脚本的 bootstrap 处理已修正，
旧审计文件保留并标为被替代，没有重跑任何 episode 或改写原始结果。

## 占卡已自动恢复

释放原占卡与两组推理启动在同一条 shell 命令内执行。各组结束后：

| 卡 | 新占卡 PID | 恢复用时 | 状态 |
|---|---:|---:|---|
| 3 | 3723276 | 3.017秒 | HOLDING，100% |
| 4 | 3723197 | 3.060秒 | HOLDING，100% |

每卡约63 GiB张量分配，显存总占用约64 GiB，分别从03:11:03 / 03:10:14 UTC
起最多保留120分钟。新进程使用各自状态文件，原 `h100-gpu.json` 指向的旧进程
已退出。提前释放时，在 H100 执行：

```bash
DIDO_ROOT=/root/wenbiao_zhao/dreamwam-sr
for gpu in 3 4; do
  "$DIDO_ROOT/DreamWAM-fresh-6c52f36/.venv/bin/python" \
    "$DIDO_ROOT/.trees/dido-hold-64471fb/scripts/sparse/gpu_hold.py" stop \
    --state "$DIDO_ROOT/gpu-hold/h100-tenpairs-after-gpu${gpu}.json"
done
```

没有删除其他进程保护，没有向其他项目发送信号。下一步由用户决定；不自动
续跑、扩样、调参或续期占卡。
