# H200 GPU6 renderer: actual launches failed before the first image

Status: RENDERER_FAILED, 2026-09-21 02:22:04 UTC
(2026-09-20 19:22:04 America/Los_Angeles). No new closed-loop episode,
inference timing run, or SR result was produced. The requested ten pairs remain
pending. The conserved effort ledger remains byte-identical at 12 charges /
12 historical attempts, SHA256
`73501eee4d96b2419684a8a1cf35b54cd86260089d9eef0a28f5df8c98ce4247`.

The user explicitly selected GPU7 inference and shared GPU6 EGL rendering,
then requested a direct GPU6 renderer launch after the first diagnostic failed.
Both launches entered the real LIBERO/robosuite native renderer. They were not
refused by resource admission. Each failed at read 1 with
`readback returned without writing its output`: 1 started read, 0 completed,
0 diagnostic steps, exit 1. Subsequent EGL_NOT_INITIALIZED messages came from
destructors after that primary failure. The second launch had no wall timeout.
Removing a time limit does not address the observed missing image.

| Diagnostic | Start UTC | End UTC | First-read elapsed | Exit |
|---|---|---|---|---|
| renderer-gpu6-shared | 02:05:54 | 02:06:10 | 13.245 s | 1 |
| renderer-gpu6-requested-0221 | 02:21:44 | 02:22:04 | 17.179 s | 1 |

Source: model `41f515a5480cdcc87a18b147ab393e9cae337e9f`, evaluator
`09300a93b2a143abf846107bcbe9e3b9abae5443`, environment script from controller
`15ef401d20fde2fb13afd0b44ab21583abb12917`. Checkpoint SHA256 (verified in
this H200 preflight):
`6c087e5b9e201f19dbe92834b470a7ee18937a18ea9adcbb49886aa1e3ac4c61`.
No model was loaded by either renderer probe; torch CUDA remained uninitialized.
Model/evaluator environments, protocol and simulator packages were unchanged.

Host: `H200-target-server`. Root:
`/data/chenjiayu/wenbiao_zhao/dreamwam-sr`.
Artifacts are under `outputs/dido-h200-paired10-20260921/` with the diagnostic
names above: `.log`, `.exit`, `-start.txt`, `-end.txt`, `-admission.xml`, and
each diagnostic's `events.jsonl` / `reads.log`.

Latest command, from `.trees/dido-eval-09300a9-h200`, with the committed H200
environment sourced and frozen MODEL_ROOT/ACTION_EVAL_ROOT/PYTHONPATH overrides:

```bash
CUDA_VISIBLE_DEVICES=6 "$EVAL_PYTHON" scripts/probe-libero-render.py \
  configs/experiments/dreamwam-dido-dense-h200-tenpairs-a.yaml \
  --output "$out/renderer-gpu6-requested-0221" --reads 80 --init-index 3 \
  --import-order benchmark-first --check-writes
```

Latest log SHA256:
`f301f79b3ca4c5de6ed560a00a0b853b3960fe64e03db022c6533b61ca8ddc19`;
events SHA256:
`0f4bb07235275c32f5cda7505d14c909683b5c3ce0fcb16768fe7513c55ee72f`;
read journal SHA256:
`3a50f1deb4a89edd89348591e063620ae0b2ea81cb4ce62c51c4e0b3400b8bad`;
admission XML SHA256:
`4d42dcb6f085764a1ca1b0e457d8b89021c9ed35ca1a8b83b1c94466a05ad2bf`.

At the latest admission GPU6 had foreign graphics PID1109733, 652 MiB, with
7% device utilization. That coexistence does not establish a root cause, and
low utilization does not establish valid readback. No foreign process was
signalled. GPU7's separately requested hold remains PID1115695, approximately
111.4 GiB allocated, 100% observed utilization, with a finite 7200-second lease
starting at Unix1789956139.9285421. Its state file is
`gpu-hold/dido-h200-gpu7-wait.json`. The holder's admission protections remain.

Next step: obtain valid native rendering before starting the twenty-episode
comparison; preserve both failures and avoid an automatic unchanged retry loop.
Inference-only replay remains possible but was not started before the user's
subsequent instructions. Do not report a speedup or a complete H200 SR.
