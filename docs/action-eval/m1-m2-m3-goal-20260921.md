# Observation budgets, dependency selection and denoising routing

Status: **implementation VERIFIED; five-pair pilot COMPLETE with quality regressions**, 2026-09-21 UTC.
Starting source: `ad0765b`. This record tracks the complete user goal: M1, M2,
adaptive M3, independently ablatable execution, traceable real-input visualizations,
server numerical checks, factor comparisons and a new matched 50-episode/arm
exploratory Spatial comparison. The user's latest amendment requests **five matched
episodes per arm first**; 50-pair testing is deferred. No historical queue is resumed.

## Frozen first-stage control

[`m1-fixed-m2-m3.json`](../../configs/sparse/m1-fixed-m2-m3.json) freezes M2 to
current first-layer AV plus backward VV support, context/guidance weights 1,
support seed ratio 0.1 and balanced frame quotas. M3 is Dense step 0, Sparse
step 5, feature Reuse at all remaining steps. All ten action updates remain.
These are executable controls, not claims of optimality or proven importance.

M1 changes only Q and KV ratios between chunks. Initial levels are Q/KV
10%/25%, 20%/50%, 30%/75%. These are **uncalibrated development choices**.
The controller sees current raw RGB from both cameras, eight-dimensional robot
proprioception and committed past observation summaries. It decides before text
encoding, VAE and denoising. No outcome, privileged state, instruction embedding,
attention, denoising latent or teacher prediction enters the decision.

RGB summaries are 32-square BOX thumbnails. Change uses the 90th percentile of
per-pixel mean absolute RGB change. Additional features are translation distance,
SO(3) orientation distance, two-finger displacement, and image/translation
deviation from the preceding two-observation linear trend. The maximum normalized
feature selects a band, thresholds 0.5/1.5. Scales and units are explicit in JSON;
they are not calibrated error bounds. Bootstrap uses high budget, upgrades are
immediate, and each one-level downgrade requires two lower-score observations.
History commits only after valid actions; episode reset discards it.

The scientific hypothesis is that these observable changes help predict
sparsification sensitivity. They do **not** establish semantic task phases,
contact detection, uncertainty calibration or safe control. Fixed-level controls
extract the same observation features and differ only in budget decisions.

## Required deliverables and current evidence

| Requirement | Status | Evidence required |
|---|---|---|
| Frozen M2/M3 identity | VERIFIED | committed config and server invariant tests |
| M1 controller and adapter diagnostics | VERIFIED | complete-history replay and real checkpoint graph/eager checks |
| M2 uniform/random/drift/AV/context controls | IMPLEMENTED; measurements pending | equal-budget factor measurements remain |
| M2 real patch/AV/executed attention capture | VERIFIED | raw arrays, actual routes, instrumentation parity |
| Adaptive M3 and fixed-schedule control | VERIFIED implementation | causal step signals, budget accounting, numerical checks |
| Structure-only reuse control | EXISTING, needs new comparison | separate semantics and measured cost |
| PNG/PDF/HTML gallery and raw JSON/NPZ | VERIFIED | IDs/axes/normalization, hashes, reproduction command |
| CPU/CUDA and real-checkpoint adapter tests | VERIFIED | source/checkpoint hashes, exact command, exit code |
| M1/M2/M3 independent and combined ablations | TODO | full predict_action timings and executed work |
| New Dense/candidate SR | five-pair pilot COMPLETE; 50 pairs deferred | Dense 5/5, M1–M3 3/5; no errors; two regressions |

Visualizations must distinguish router probes from actual attention executed by
the transformer. Preserve joint action-plus-video softmax probabilities; label
heads, action queries, layers, steps and aggregates. Show future-frame latent
grids separately from the real first frame. Spatial patch boxes are coordinates,
not isolated VAE receptive fields or pixel-level causal attribution.

All implementation is edited/committed/pushed locally then pulled on the server;
runs use clean detached worktrees. Reuse existing Python 3.10.20 / torch
2.7.1+cu126 environments without installation. Source dependency SHA256 before work:

- `pyproject.toml`: `2d717d498ffd31e612a6b523ee4428b8788bd7b7f9f804c33776114f64afafac`
- `requirements.txt`: `22f662d3eb88634277dea66d81114368eb9f15ce4a5c47e21f860bcd9ffd4448`
- H100 lock: `435692180f4d5a28e47d00be00cba35ea83cca8f9cdbd2566b96eec819098354`
- Reference checkpoint: `6c087e5b9e201f19dbe92834b470a7ee18937a18ea9adcbb49886aa1e3ac4c61`

H100 admission at 2026-09-21 11:31:57 UTC: authorized cards 3/4/5 have external
jobs, approximately 24 GiB each and 99/99/100% utilization. No GPU job launched.
CPU tests and existing-data work remain possible. Next: commit/deploy M1 and
run server CPU integration tests while implementing visualization instrumentation.

## Progress and H200 allocation, 2026-09-21 UTC

- **VERIFIED**, source `d73701ea948f15caaff2822c0830e1e109b43657`:
  H100 server, CPU sparse suite, exit 0; 364 passed, 19 CUDA-only skips,
  16 subtests. Command: `CUDA_VISIBLE_DEVICES= OMP_NUM_THREADS=1 MKL_NUM_THREADS=1
  OPENBLAS_NUM_THREADS=1 PYTHONPATH=. <reference-python> -m pytest tests/sparse -q`.
  Artifacts: H100 `outputs/m1-m2-m3-20260921/m1-d73701e/cpu-tests.{log,exit}`.
  Environment reports Python 3.10.20 / torch 2.7.1+cu126; dependency hashes unchanged.
  Checkpoint not loaded by these fixture tests. Real-checkpoint checks remain pending.
- **VERIFIED**, source `53bc2b2`: CPU M1/trace/trajectory tests, exit 0;
  31 passed, one CUDA skip. Command: same environment, `-m pytest
  tests/sparse/test_chunk_budget.py tests/sparse/test_hybrid_trace.py
  tests/sparse/test_trajectory_export.py -q`.
  Artifacts: H100 `outputs/m1-m2-m3-20260921/trace-53bc2b2/cpu-tests.{log,exit}`.
  These include actual fixture-transformer Q/K capture, joint softmax checks,
  route mapping and bitwise actions with/without instrumentation. They are not
  real-checkpoint visual evidence or GPU timing.
- **VERIFIED_OBSERVATION_REPLAY**, source `53bc2b2`, exit 0: all 29 contiguous
  inputs from three previously completed Dense development episodes exported with
  `export_trajectory_inputs.py --all-calls` and replayed by
  `replay_observation_budgets.py`. Mean CPU controller time 1.192 ms in that replay.
  Artifacts: H100 `outputs/m1-m2-m3-20260921/{observations-dev3,m1-replay}`.
  No model execution or SR measured. The first/middle/last-only exports are rejected
  as incomplete history. Thresholds remain uncalibrated.

The user subsequently moves this effort to H200 and explicitly authorizes
**GPUs 0, 1, 4, 5**. At **11:54:22 UTC**, all four holders were identity-checked
alive and `HOLDING`, each approximately 114,823 MiB total device memory and 99%
utilization. GPUs 2/3 remained approximately 3 MiB / 0%; other users' processes
on 6/7 were untouched. This is resource preparation, not an experiment result.

Existing committed hold implementation: `64471fbbdc33e27695d23709385ab5c5f8169446`,
`scripts/sparse/gpu_hold.py`, launched with separate state files, `--minutes 120
--fraction 0.8 --matrix 8192`. Hold processes: GPU0 PID2625748, GPU1 PID2625867,
GPU4 PID2625899, GPU5 PID2626002. Always revalidate token/start ticks using the
script's `status`/`stop` commands before signalling; these PIDs alone are not
authorization to kill a subsequently reused PID. Initial readiness is around
11:54 UTC and initial leases expire around 13:54 UTC.

State/admission/log root on H200:
`/data/chenjiayu/wenbiao_zhao/dreamwam-sr/gpu-hold/m1-m2-m3-20260921-1153/`.
Next: release this effort's GPU4 holder for CUDA checks and real-checkpoint
visual capture, keep the other holders bounded, and continue adaptive M3 and
the complete goal. No historical SR queue is restarted.

## Completed capture and hardware steering, 2026-09-21 UTC

- **VERIFIED**, source `53bc2b2`, H200 GPU 4, full CUDA sparse suite: 389 passed
  and 16 subtests passed, exit 0 (19.33 s). Exact command, UTC timestamps and
  admission snapshots: H200 `outputs/m1-m2-m3-20260921/cuda-53bc2b2/`.
  These fixture tests do not load the reference checkpoint.
- **VERIFIED_REAL_CHECKPOINT_TRACE**, source `cebba83`, H200 GPU 4, exit 0.
  All 29 complete-history calls from three development episodes match their
  uninstrumented actions bitwise; nine first/middle/last chunks include actual
  VAE-input checks, layer 0/29 and step 0/5/9 attention, patch IDs, key/query masks,
  cache ages, JSON/NPZ, PNG/PDF and HTML. Artifacts:
  H200 `outputs/m1-m2-m3-20260921/trace-cebba83/`; launch log and exit beside it
  in `trace-cebba83-validation/`. Checkpoint SHA256 remains
  `6c087e5b9e201f19dbe92834b470a7ee18937a18ea9adcbb49886aa1e3ac4c61`.
  Command: `capture_hybrid_trace.py --inputs <observations-dev3/manifest.json>
  --out-dir <trace-cebba83>` under the unchanged model interpreter, one explicitly
  selected CUDA GPU and one CPU thread per math library. No timing or SR claim.
- **NEGATIVE DEVELOPMENT RESULT**: initial M1 thresholds 0.5/1.5 select high
  budget for all 29 observations. Non-bootstrap scores range 2.121–4.967. The
  initial options/results are retained. Observation-only development quantile
  calibration is implemented in `calibrate_observation_budget.py`, not yet run;
  it does not establish action sensitivity or task phase recognition.
- Source `6ce6ce7` implements adaptive M3 with drift/age signals before visual
  layers, a hard total Q-row cap, explicit probe cost, and a seeded random M2
  comparator. New server tests and combined real-checkpoint checks remain pending.

At **12:12:50 UTC** the user's H200-hold pause was applied: all four owned
holders reported `RELEASED`, no CUDA processes remained, and GPUs 0/1/4/5
reported zero used MiB. No automatic holder restart is authorized.

At **12:18:25–29 UTC**, sequential H100 GPU 0/1 BF16 GEMM probes passed under
the unchanged torch 2.7.1+cu126 environment; no checkpoint was loaded. Raw
admission/probe records: H100 `outputs/h100-gpu01-check-20260921-1218/`.
The user now requests **H100 GPU 1** for ongoing work. At 12:20:40 UTC GPU 1
reported 38,914 MiB used, 42,168 MiB free and 0% instantaneous utilization;
GPU 0 was empty. This is explicitly shared usage, not a reservation. Next:
deploy committed code, run bounded CUDA tests on GPU 1, then real-model replay
subject to fresh admission. Leave GPU 0 unused and do not resume historical queues.

**Superseding placement, 12:23–12:25 UTC:** the user corrects the requested card
to H100 GPU 0, confirms coordination to stop the previous jobs on 3–5, and
requests immediate holding of available cards. All six known previous PIDs
had exited when inspected; no kill signal was sent. GPU 3 subsequently had new
external load. At 12:25:17 UTC, separate holders on 0/4/5 reported `HOLDING`
and identity-matched live PIDs 894182/894209/894196, each 65,165 MiB device use.
Command: existing committed `64471fb` `gpu_hold.py start --gpus <0|4|5>
--minutes 120 --fraction 0.8 --matrix 8192 --state <per-GPU-state>`; all launch
and verification commands exited 0. States and admission artifacts are in
H100 `gpu-hold/m123-h100-20260921-1225/`; leases expire around 14:25 UTC.
GPU 1/2 are unused by this effort. H200 holds remain paused.

**VERIFIED**, source `ad2969a`, H100 GPU 1 shared placement before the correction:
405 sparse tests and 16 subtests passed in 20.47 s, exit 0. No checkpoint loaded.
Artifacts: H100 `outputs/m1-m2-m3-20260921/h100-ad2969a/`; command: unchanged
reference interpreter, `-m pytest tests/sparse -q`, explicit GPU-1 UUID and one
thread per math library. The observation-only calibration command then proposed
score tertiles **3.2113290329774213 / 3.6437906324863434** from 26 non-bootstrap
development calls, exit 0. Proposal/report are in `h100-ad2969a/m1-calibration/`.
These thresholds have not yet been adopted for model execution. Next: locally
commit the reviewed calibration options, then hand GPU 0 from its bounded holder
to combined real-checkpoint verification; preserve independent confirmation data.

The proposed score tertiles are now frozen in
`configs/sparse/m1-calibrated-fixed-m2-m3.json`; M2 and fixed M3 remain identical
to the first-stage control. `configs/sparse/m123-calibrated.json` adds only the
adaptive step router. These are development configurations awaiting real-model
verification, not validated task-phase recognition or SR-preserving policies.
The uncalibrated originals and their all-high-budget result remain unchanged.

The H200 gallery contains **135 PNG / 135 PDF images across nine captured
chunks**, with nine tensor archives. Visual inspection of task 0 / init 1 /
chunk 5 shows seven of its top eight observed read-score cells on the nearly
uniform bottom row of the wrist image (IDs 91–97). This is a reason to test
the attention proxy, not evidence that these cells are task-critical. Nominal
image cells do not isolate the VAE's overlapping receptive fields. Matched
uniform/random/drift/AV-only controls and action interventions remain necessary.

## Calibrated capture, desktop export and five-pair pilot

**VERIFIED_REAL_CHECKPOINT_TRACE**, model `f0cb22de8d1ce85c6f9add024f77565140a9ea1b`,
H100 GPU 0, checkpoint SHA256 as above. Fixed-M3 capture ran
**12:31:00–12:37:14 UTC**, adaptive-M3 capture **12:37:19–12:43:56 UTC**;
both exited 0. Each checks all 29 chronological development requests with/without
instrumentation, and exports nine chunks with 135 PNGs / 135 PDFs / tensor archives.
The trace backend is eager; CUDA Graph equivalence is checked separately below.
Commands: `capture_hybrid_trace.py --inputs <observations-dev3/manifest.json>
--options configs/sparse/<m1-calibrated-fixed-m2-m3|m123-calibrated>.json
--out-dir <fixed-m3|adaptive-m3>` with the unchanged model interpreter and explicit
GPU-0 UUID. Artifacts: H100 `outputs/m1-m2-m3-20260921/h100-gpu0-f0cb22d/`.
Calibrated observation replay selected high/medium/low on **19/8/2** calls;
its mean controller cost was 1.217 ms. This is development replay, not a quality
or inference-speed result. Attention pictures remain descriptive evidence.

**VERIFIED desktop transfer**, 2026-09-21 UTC: SCP copied all **411 generated
PNGs** to `/mnt/c/Users/genev/Desktop/DreamWAM-M123-20260921/`:
135 in `H200/`, 276 in `H100/` (270 trace pictures and six budget-history plots).
Each cohort includes `SHA256SUMS.txt`; both `sha256sum -c --quiet` checks exited 0.
The compressed source archives are retained beside the extracted directories.

The user requests **five episodes first**. The frozen pair uses tasks 0–4 × init 0,
five episodes per arm, and preserves the `dreamwam-release-v1` protocol (400-step
limit, wait 30, replan 10, 256-square cameras, seed 42). This is a development
pilot, not an independent confirmation cohort. No 50-pair or historical job starts.
Both model configs are committed in `configs/action_eval/m123-*-pilot5-osmesa.yaml`.
Dense shares prompt caching, conditioned-frame reuse and transformer graphs;
candidate adds the frozen calibrated M1, AV–VV selection and adaptive M3.

**VERIFIED adapter preflight**, **12:47:42–12:49:53 UTC**, model
`7300f345b65bb2413975de305813b496d9412495`:
`verify_chunk_adapter.py --inputs <observations-dev3/manifest.json> --options
configs/sparse/m123-calibrated.json --out-dir <pilot5-7300f34/adapter>` exited 0.
All **116 real-checkpoint requests** (29 complete-history inputs × two repeats ×
two arms) are bitwise equal to their own uncached eager reference, including M1
history/decisions, actual M3 operations, executed/read row counts, prompt reset
and stable fingerprints. Setup is included in these diagnostic durations; they
are not a latency benchmark. Checkpoint hash is the unchanged reference above.
The admission/fingerprint test file passes 10 tests (0.22 s, exit 0). Both configs
pass validate/doctor; native OSMesa `describe` reports expected RGB/action shapes,
robosuite 1.4.1 and MuJoCo 3.3.2. Logs are in
H100 `outputs/m1-m2-m3-20260921/pilot5-7300f34/`.

**RUNNING pilot**, start **12:50:28 UTC**, evaluator
`e5a5cc5e05d381b0cc5bafbe4e000343c4b6e29f` (existing observation-recording checkout;
unchanged simulator environment). GPU 0 is explicitly shared: at Dense admission
it has 28,241 MiB used, 52,841 MiB free and 7% utilization; physical UUID
`GPU-7f893bc3-9a65-24c1-47dd-1e4428cec01f`. Owned GPU-5 holder was identity-checked
and released at 12:48:31 UTC, leaving GPU 5 unused. GPU-4 holder remains bounded
by its original approximately 14:25 UTC lease. The automatic GPU-0 holder return
after capture was rejected by its empty-card admission check; it did not start.

Launch command under the frozen evaluator interpreter and its `src` plus LIBERO
on `PYTHONPATH`: `scripts/sparse/run_fresh_token_pair.py --eval-root <evaluator>
--model-root <model> --out-dir <pilot5-7300f34/sr> --adapter-report <adapter/report.json>
--dense-config <model/configs/action_eval/m123-dense-pilot5-osmesa.yaml>
--sparse-config <model/configs/action_eval/m123-candidate-pilot5-osmesa.yaml>
--planned-episodes 5 --render-backend osmesa --first-arm dense --authorized-gpus
0 3 4 5 --wall-seconds 1800 --admission-seconds 120 --stop-grace-seconds 180`.
The source environment script is `deployment/h100/fresh-token-osmesa.env.sh`,
with `MODEL_ROOT` overridden to the frozen model worktree and `MODEL_PYTHON` set
to the unchanged reference environment. Native-write checks stay enabled for every
episode. Exit status and SR are pending; next: inspect all ten terminal outcomes
and run `paired_sr.py`, preserving errors separately and withholding incomplete SR.

### Completed five-pair results, 2026-09-21 12:59:49 UTC

**VERIFIED / PILOT_PAIR_COMPLETE**, frozen model/evaluator/checkpoint identities and
command above. Dense ran **12:50:33–12:53:45 UTC**; candidate ran
**12:54:20–12:59:49 UTC**. Both exited 0, both executed fingerprints matched, and
neither arm required forced cleanup. All ten episodes reached benchmark-owned
outcomes; there were no errors, cancellations or missing episodes.

| Spatial task ID | Dense | M1–M3 | Candidate termination |
|---|---|---|---|
| 0 | success | success | 75 executed steps |
| 1 | success | failure | 400-step limit |
| 2 | success | success | 107 executed steps |
| 3 | success | failure | 400-step limit |
| 4 | success | success | 145 executed steps |

The complete pilot rates are **5/5 (100%) versus 3/5 (60%)**, a descriptive
−40 percentage-point difference in these five identities. Wilson intervals are
56.6–100% and 23.1–88.2%; task-stratified bootstrap is withheld because each task
has only one initial state. This is a negative development pilot, not a precise
benchmark estimate or evidence of SR preservation. Initial state, agentview and
wrist hashes match on **all five pairs** before policy-dependent actions.

Closed-loop warm `predict_action` means are **328.599 ms Dense** (44 calls) and
**321.672 ms candidate** (109 calls), ratio **1.0215×**. Including first calls gives
362.624 / 340.155 ms (1.0661×); first calls contain lazy graph setup and prompt
misses. These are different trajectories on a shared GPU with varying external
load, so they do not establish a controlled acceleration result. Mean episode
wall time increases from 29.094 s to 56.566 s, including simulator/rendering and
the candidate's two full-horizon failures. No 2× claim applies to this M1–M3 run.

Candidate diagnostics contain 114 chunks: **31 high / 11 medium / 72 low**.
Actual step totals are **114 Dense / 641 Sparse / 385 Reuse**; every prediction
updates all 300 action layer-steps. Failed tasks 1 and 3 use low budget on 35/40
and 34/40 chunks, respectively. This correlation does not establish cause:
stalled observations may themselves trigger lower budgets. Another design limit
is that M1 currently changes per-refresh Q/KV quotas, while M3's total extra-Q cap
is fixed. More low-quota refreshes can consume nearly the same total visual work:
mean computed video token-layers are 15,120 / 15,900 / 16,830 for low/medium/high.
Independent factor tests and a true chunk-total budget remain important follow-ups.

Postprocessing commands, all exit 0 at approximately **13:00 UTC**, under the
unchanged model interpreter, from the frozen model worktree:

```sh
python scripts/sparse/paired_sr.py --dense "$OUT/sr/dense/run" --sparse "$OUT/sr/sparse/run" --label m123-pilot5 --json "$OUT/paired-sr.json"
python scripts/sparse/summarize_policy_timings.py --dense "$OUT/sr/dense/run" --sparse "$OUT/sr/sparse/run" --out "$OUT/timings.json"
python scripts/sparse/audit_initial_inputs.py --reference "$OUT/sr/dense/run" --candidate "$OUT/sr/sparse/run" --out "$OUT/initial-input-audit.json"
```

`OUT` is the H100 `outputs/m1-m2-m3-20260921/pilot5-7300f34` directory above.
`executed-routing.json` is a direct extraction of accepted per-call diagnostics,
including source file hashes and complete budget/operation sequences. Evidence
archive `pilot5-7300f34-evidence.tar.gz` has SHA256
`d6189e257299135ac8ef9da78ef23f7b876903dafe579755b06e90927b46ac46`.
Original episode observations, actions, manifests, logs and native-read checks
are retained. Next: diagnose the regressions and measure matched-input speed on
an exclusively held GPU before scaling; 50-pair testing remains deferred.

At **12:55:12 UTC**, newly empty authorized GPU 3 was reserved using the existing
identity-checked holder, state `gpu3-later-empty.json`, PID 1095587, 89-minute
lease within the original approximately 14:25 UTC deadline. GPU 4 retains its
original bounded holder; GPU 5 was left unused by this effort. This additional
cross-GPU load is another reason the pilot timing is descriptive only. GPU 0's
model processes ended with the pilot; its external processes were untouched.
