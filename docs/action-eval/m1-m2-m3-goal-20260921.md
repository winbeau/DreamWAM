# Observation budgets, dependency selection and denoising routing

Status: **PLANNED / implementation in progress**, 2026-09-21 UTC.
Starting source: `ad0765b`. This record tracks the complete user goal: M1, M2,
adaptive M3, independently ablatable execution, traceable real-input visualizations,
server numerical checks, factor comparisons and a new matched 50-episode/arm
exploratory Spatial comparison. No historical queue is resumed.

## Frozen first-stage control

[`m1-fixed-m2-m3.json`](../../configs/sparse/m1-fixed-m2-m3.json) freezes M2 to
current first-layer AV plus backward VV support, context/guidance weights 1,
support seed ratio 0.1 and balanced frame quotas. M3 is Dense step 0, Sparse
step 5, feature Reuse at all remaining steps. All ten action updates remain.
These are executable controls, not claims of optimality or proven importance.

M1 changes only Q and KV ratios between chunks. Initial levels are Q/KV
10%/25%, 20%/50%, 30%/75%. These are **uncalibrated development choices**.
The controller sees current raw RGB from both cameras, eight-dimensional robot
proprioception and committed past observation summaries. It decides before text
encoding, VAE and denoising. No outcome, privileged state, instruction embedding,
attention, denoising latent or teacher prediction enters the decision.

RGB summaries are 32-square BOX thumbnails. Change uses the 90th percentile of
per-pixel mean absolute RGB change. Additional features are translation distance,
SO(3) orientation distance, two-finger displacement, and image/translation
deviation from the preceding two-observation linear trend. The maximum normalized
feature selects a band, thresholds 0.5/1.5. Scales and units are explicit in JSON;
they are not calibrated error bounds. Bootstrap uses high budget, upgrades are
immediate, and each one-level downgrade requires two lower-score observations.
History commits only after valid actions; episode reset discards it.

The scientific hypothesis is that these observable changes help predict
sparsification sensitivity. They do **not** establish semantic task phases,
contact detection, uncertainty calibration or safe control. Fixed-level controls
extract the same observation features and differ only in budget decisions.

## Required deliverables and current evidence

| Requirement | Status | Evidence required |
|---|---|---|
| Frozen M2/M3 identity | IMPLEMENTED, unverified | committed config, canonical hashes, invariant tests |
| M1 controller and adapter diagnostics | IMPLEMENTED, unverified | server tests, executed counts, real-checkpoint sequence |
| M2 uniform/random/drift/AV/context controls | PARTIAL | random control, equal-budget measurements |
| M2 real patch/AV/executed attention capture | TODO | raw arrays, actual routes, instrumentation parity |
| Adaptive M3 and fixed-schedule control | TODO | causal step signals, budget accounting, numerical checks |
| Structure-only reuse control | EXISTING, needs new comparison | separate semantics and measured cost |
| PNG/PDF/HTML gallery and raw JSON/NPZ | TODO | IDs/axes/normalization, hashes, reproduction command |
| CPU/CUDA and real-checkpoint adapter tests | TODO | source/checkpoint hashes, exact command, exit code |
| M1/M2/M3 independent and combined ablations | TODO | full predict_action timings and executed work |
| New Dense/candidate 10 tasks x 5 initial states | TODO | complete benchmark-owned outcomes, errors separate |

Visualizations must distinguish router probes from actual attention executed by
the transformer. Preserve joint action-plus-video softmax probabilities; label
heads, action queries, layers, steps and aggregates. Show future-frame latent
grids separately from the real first frame. Spatial patch boxes are coordinates,
not isolated VAE receptive fields or pixel-level causal attribution.

All implementation is edited/committed/pushed locally then pulled on the server;
runs use clean detached worktrees. Reuse existing Python 3.10.20 / torch
2.7.1+cu126 environments without installation. Source dependency SHA256 before work:

- `pyproject.toml`: `2d717d498ffd31e612a6b523ee4428b8788bd7b7f9f804c33776114f64afafac`
- `requirements.txt`: `22f662d3eb88634277dea66d81114368eb9f15ce4a5c47e21f860bcd9ffd4448`
- H100 lock: `435692180f4d5a28e47d00be00cba35ea83cca8f9cdbd2566b96eec819098354`
- Reference checkpoint: `6c087e5b9e201f19dbe92834b470a7ee18937a18ea9adcbb49886aa1e3ac4c61`

H100 admission at 2026-09-21 11:31:57 UTC: authorized cards 3/4/5 have external
jobs, approximately 24 GiB each and 99/99/100% utilization. No GPU job launched.
CPU tests and existing-data work remain possible. Next: commit/deploy M1 and
run server CPU integration tests while implementing visualization instrumentation.

## Progress and H200 allocation, 2026-09-21 UTC

- **VERIFIED**, source `d73701ea948f15caaff2822c0830e1e109b43657`:
  H100 server, CPU sparse suite, exit 0; 364 passed, 19 CUDA-only skips,
  16 subtests. Command: `CUDA_VISIBLE_DEVICES= OMP_NUM_THREADS=1 MKL_NUM_THREADS=1
  OPENBLAS_NUM_THREADS=1 PYTHONPATH=. <reference-python> -m pytest tests/sparse -q`.
  Artifacts: H100 `outputs/m1-m2-m3-20260921/m1-d73701e/cpu-tests.{log,exit}`.
  Environment reports Python 3.10.20 / torch 2.7.1+cu126; dependency hashes unchanged.
  Checkpoint not loaded by these fixture tests. Real-checkpoint checks remain pending.
- **VERIFIED**, source `53bc2b2`: CPU M1/trace/trajectory tests, exit 0;
  31 passed, one CUDA skip. Command: same environment, `-m pytest
  tests/sparse/test_chunk_budget.py tests/sparse/test_hybrid_trace.py
  tests/sparse/test_trajectory_export.py -q`.
  Artifacts: H100 `outputs/m1-m2-m3-20260921/trace-53bc2b2/cpu-tests.{log,exit}`.
  These include actual fixture-transformer Q/K capture, joint softmax checks,
  route mapping and bitwise actions with/without instrumentation. They are not
  real-checkpoint visual evidence or GPU timing.
- **VERIFIED_OBSERVATION_REPLAY**, source `53bc2b2`, exit 0: all 29 contiguous
  inputs from three previously completed Dense development episodes exported with
  `export_trajectory_inputs.py --all-calls` and replayed by
  `replay_observation_budgets.py`. Mean CPU controller time 1.192 ms in that replay.
  Artifacts: H100 `outputs/m1-m2-m3-20260921/{observations-dev3,m1-replay}`.
  No model execution or SR measured. The first/middle/last-only exports are rejected
  as incomplete history. Thresholds remain uncalibrated.

The user subsequently moves this effort to H200 and explicitly authorizes
**GPUs 0, 1, 4, 5**. At **11:54:22 UTC**, all four holders were identity-checked
alive and `HOLDING`, each approximately 114,823 MiB total device memory and 99%
utilization. GPUs 2/3 remained approximately 3 MiB / 0%; other users' processes
on 6/7 were untouched. This is resource preparation, not an experiment result.

Existing committed hold implementation: `64471fbbdc33e27695d23709385ab5c5f8169446`,
`scripts/sparse/gpu_hold.py`, launched with separate state files, `--minutes 120
--fraction 0.8 --matrix 8192`. Hold processes: GPU0 PID2625748, GPU1 PID2625867,
GPU4 PID2625899, GPU5 PID2626002. Always revalidate token/start ticks using the
script's `status`/`stop` commands before signalling; these PIDs alone are not
authorization to kill a subsequently reused PID. Initial readiness is around
11:54 UTC and initial leases expire around 13:54 UTC.

State/admission/log root on H200:
`/data/chenjiayu/wenbiao_zhao/dreamwam-sr/gpu-hold/m1-m2-m3-20260921-1153/`.
Next: release this effort's GPU4 holder for CUDA checks and real-checkpoint
visual capture, keep the other holders bounded, and continue adaptive M3 and
the complete goal. No historical SR queue is restarted.
