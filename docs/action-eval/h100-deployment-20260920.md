# H100 deployment and independent fresh-token screening

Status: **DEPLOYING**, 2026-09-20 UTC. The user authorized `ssh h100-server`,
deployment under `wenbiao_zhao`, and experiments there, then explicitly required
Hugging Face downloads through **hf-mirror.com**. This is a separate deployment
from the deferred H200 SR runs; preserve their data and do not resume their queues.

The discovered root is `/root/wenbiao_zhao/dreamwam-sr` on
`zhengchen-ubuntu-8xh100-05`. The container exposes six H100 80GB devices, driver
590.48.01. Initial placement is policy GPU 3 (`GPU-c0af33a9-498c-ff7c-bb56-e9992ccded30`),
renderer GPU 4 (`GPU-3af22086-0ac4-4277-1486-96909b440d1c`), and GPU 5 left unused.
Recheck occupancy at each launch. The bounded controller now accepts explicit
`--authorized-gpus 3 4 5`; it also rejects an allegedly empty renderer whose
memory use contradicts the process list, important with container telemetry.

Frozen model: `6c52f362a975e560707b905c1c57cb46a1b9b0a8`.
Frozen evaluator/configs: `e0d9e800bf621661cac99388f9e37002b9e8fdfa`.
LIBERO: `8f1084e3132a39270c3a13ebe37270a43ece2a01`, clean upstream checkout.
The platform already loads its legacy initial states with `weights_only=False`,
so the reference host's helper-only LIBERO patch need not be copied.

Both repositories and LIBERO were deployed through Git. The first attempt to
relay assets and third-party packages over SSH was too slow and was stopped;
partial files were isolated and are not used for evaluation. Model assets were
then fetched directly from hf-mirror at pinned revisions:

- `hustvl/DreamWAM`: `70e0e2ac0a92d5375fd6dc3ebb0a1c71c71cfebd`.
- `Wan-AI/Wan2.2-TI2V-5B`: `921dbaf3f1674a56f47e83fb80a34bac8a8f203e`.

All 11 required model/tokenizer/captured-input files passed byte-size and SHA256
comparison with the reference H200 assets. Checkpoint SHA256:
`6c087e5b9e201f19dbe92834b470a7ee18937a18ea9adcbb49886aa1e3ac4c61`.
The asset/download records are under server `outputs/deploy-h100-20260920/`.

Python remains 3.10.20. The model's reference package inventory was captured on
H200; H100 generated the full hash-pinned
[`model-requirements.lock`](../../deployment/h100/model-requirements.lock), which
was returned for local review, committed, and pulled before installation with
`uv pip sync --require-hashes`. The evaluator uses its unchanged committed
`uv.lock` with `uv sync --locked --no-build-isolation`. Isolated build tooling is
explicitly pinned to setuptools 84.0.0 and wheel 0.45.1; the SDK is installed
from the frozen evaluator checkout. New environments are separate from other jobs.

The container initially lacked EGL libraries. Graphics-only libraries matching
the **existing** 590.48.01 driver were extracted into the deployment's private
`vendor/` directory; no system driver installation or restart was performed.
The official NVIDIA bundle SHA256 is
`b9e2f80693781431cc87f4cd29109e133dcecb50a50d6b68d4b3bf2d696bd689`.
Private Ubuntu libegl1/libopengl0 packages are pinned to 1.7.0-1build1, matching
the installed GLVND version. See NVIDIA's
[component inventory](https://download.nvidia.com/XFree86/Linux-x86_64/590.48.01/README/installedcomponents.html)
and [container graphics capabilities](https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/latest/docker-specialized.html).
The private EGL enumeration check passed: six EGL indices map one-to-one to
CUDA ordinals 0–5. This establishes device mapping, not successful RGB readback.

Next gates: package/version audit, CPU/CUDA tests, real-checkpoint adapter parity,
bounded native RGB smoke, then the unchanged 10 Spatial tasks × five initial
states for **50 Dense and 50 fresh-token episodes** under `dreamwam-release-v1`.
All denoising steps, action horizon, RNG and checkpoint remain fixed. H100 timing
and SR must be measured independently; H200's 1.191× does not transfer by assumption.
No H100 latency or SR result is claimed yet. Errors are not task failures.
