# Exploratory 50-episode screening of guided visual sparsity

Status at **2026-09-20 06:21:46 UTC: PAUSED BY USER; GPUs RELEASED**.
The user asked to release the cards for others and organize existing results.
Do not resume or launch GPU experiments until instructed. Dense preserves
**5/50 accepted outcomes, all successes**; Sparse has not started. Incomplete
quick50 SR remains null. The original full-run stop remains verified.
The user requested 50 episodes per candidate to select a working method quickly,
with unified full testing deferred until the method is chosen. This is an
exploratory subset, not a reproduction of the 500-episode Spatial benchmark.

## Scope and preserved evidence

Both new arms cover Spatial tasks **0–9 × initial states 0–4**, once each:
**50 episodes for matched Dense and 50 for guided Sparse**. They use independent
new manifests. The scientific protocol remains `dreamwam-release-v1`: max 400,
wait 30, replan 10, 256×256 cameras, seed 42. The model retains the same Joint
checkpoint, bf16, horizon 32, ten denoising steps and fixed-per-predict RNG.

The [measured candidate](prompt-cache-single-factor-20260920.md) achieves
**1.872× full warm `predict_action` acceleration**, 257.20 → 137.37 ms against
equally optimized Dense. This does not measure whole-episode throughput.
Both arms use exact prompt reuse and transformer graphs; Dense also reuses
invariant conditioned-frame tokens. Sparse runs a dense visual anchor, refreshes
30/294 action-guided visual tokens at step 5, and reuses visual computation at
the other eight steps. All action updates remain active. Head × Stage
classification is not enabled in this candidate.

At the user-requested stop, the earlier full Spatial Dense run had **129/500
outcomes, all successes**. Its queued Sparse run had not started. The legacy
Long recovery had **15/500 outcomes, all successes**. Both policy workers are
gone. The before/after audit confirms every previously accepted result and the
original manifests/resolved configurations are unchanged. Full-run SR remains
null. Raw stop evidence is under
[`evidence/quick50-20260920/`](evidence/quick50-20260920/).
The signal log's first descendant list also contains the separately identified
Long descendants because the temporary list was extended in place; the recorded
PPIDs and the second controller entry identify their actual parent. All signalled
processes belong to these two owned jobs; no other evaluation was stopped.

## Frozen implementation and launch plan

- Model: `04163905f047169a159b3e366d679afc86392757`, unchanged from the
  [completed 15-pair pilot](prompt-cache-paired-20260920.md).
- Evaluator/configs: `2feed4900d43dc52f4b29936ab76ab3e5847a666`.
- Checkpoint SHA256:
  `6c087e5b9e201f19dbe92834b470a7ee18937a18ea9adcbb49886aa1e3ac4c61`.
- Configs: `dreamwam-prompt-conditioned-quick50.yaml` and
  `dreamwam-prompt-guided-quick50.yaml` in action-eval `configs/experiments/`.
- Planned placement: policy GPU 3 (`GPU-9aa453a3-63f9-ea74-f4ce-7d119e9e69ba`),
  renderer GPU 4 (`GPU-490b4a76-6210-31b9-4e03-838a113cf5f4`) for both arms.
  Recheck admission and leave another available card unused by this task.
  The separate effort's renderer GPUs 0/6/7 remain untouched.
- Reuse existing environments with no installs, sync or dependency changes.
  Include both the frozen evaluator `src` and external LIBERO on `PYTHONPATH`.
- Run the existing native-read checker and verify executed worker fingerprints.
  Error attempts remain distinct from task failures. No uncontrolled retry loop.

Server output root: `outputs/quick50-20260920/`. Both configs passed validation
and doctor. Platform tests exited 0 in **71.55 s** (199 passing cases), and the
generated schema matches the committed bytes. The recorded dependency hashes
are unchanged; no environment install/sync occurred. The expected pair duration
was approximately 20–40 minutes, with a **3,600-second limit per arm**.

Controller **476036** launched Dense at **06:19:56 UTC** on the recorded GPU 3/4
placement and verified the actual worker fingerprint. The user pause was applied
with SIGINT to this owned controller. Its cleanup stopped its runner and policy;
the **06:21:46 UTC** audit found no live owned descendants and no processes on
GPU 3 or GPU 4. All five accepted result hashes are unchanged. No Sparse run was
launched. The generic controller records `ERROR: KeyboardInterrupt()` and omits
its arm-level exit field because the interruption preceded that assignment;
this is the requested operator pause, not an observed model/task failure. Raw
records remain unedited. Exact commands, inventories, interruption and process
identities are in `quick50-pair-controller-launch.json`, `quick50-pair-gpus.jsonl`
and `user-pause-audit.json`. A SHA256 inventory covers every preserved artifact.

After all 50 identities per arm reach valid terminal outcomes, report the subset
SRs, paired regressions/gains and uncertainty. Initial observations are audited
again: the preceding pilot matched proprio but did not reproduce image bytes,
so equal outcomes alone do not establish SR preservation. No acceptable SR-loss
tolerance or non-inferiority margin has been assumed. Full evaluation is a later
phase after method selection.
